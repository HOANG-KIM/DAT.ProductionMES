namespace ProductionMES.Application.Tests;

/// <summary>
/// Test rỗng để xác nhận pipeline xUnit hoạt động đúng ở bước scaffold ban đầu.
/// Sẽ được thay thế bằng test case thật khi có business logic cụ thể (service, validator...).
/// </summary>
public class SmokeTests
{
    [Fact]
    public void Pipeline_Test_Chay_Duoc()
    {
        Assert.True(true);
    }
}
