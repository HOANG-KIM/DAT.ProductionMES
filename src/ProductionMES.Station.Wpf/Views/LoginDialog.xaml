<Window x:Class="ProductionMES.Station.Wpf.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập"
        Width="380" SizeToContent="Height" MinHeight="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Đăng nhập" FontSize="16" FontWeight="Bold" Margin="0,0,0,4"/>


        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Tài khoản" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="6"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,6">
            <TextBlock Text="Mật khẩu" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Padding="6" KeyDown="PasswordBox_KeyDown"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding ErrorMessage}" Foreground="{StaticResource StatusNgBrush}" TextWrapping="Wrap" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButtonElement" Content="Hủy" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="LoginButton" Content="Đăng nhập" Width="110" IsDefault="True" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
