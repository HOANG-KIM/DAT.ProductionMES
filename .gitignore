# Build results
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Dd]ebug[Pp]ublic/
[Rr]elease/
x64/
x86/
[Ll]og/
[Ll]ogs/

# Visual Studio / Rider
.vs/
.idea/
*.user
*.userosscache
*.suo
*.userprefs

# NuGet
*.nupkg
**/packages/*
!**/packages/build/
project.lock.json
project.fragment.lock.json
artifacts/

# Local secrets / per-environment config (giữ lại appsettings.json mẫu, loại trừ bản có secret thật)
appsettings.*.local.json
appsettings.Development.json
**/secrets.json
*.pfx

# SQLite local queue (dữ liệu cục bộ tại trạm, không commit)
*.db
*.db-shm
*.db-wal

# US-24: file mẫu tem in (template .xlsx) tải lên qua PackingTemplateStorage — dữ liệu runtime, không commit
# nội dung thật (giữ thư mục qua .gitkeep, giống appsettings.Development.json không commit nhưng vẫn cần chạy được).
src/ProductionMES.Api/App_Data/PackingTemplates/*
!src/ProductionMES.Api/App_Data/PackingTemplates/.gitkeep

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml

# Claude Code local settings (nếu phát sinh sau này)
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store
